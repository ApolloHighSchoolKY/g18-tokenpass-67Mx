public TokenPass(int playerCount){
  /*  to be answered in part (a) */
  Random tokenNum = new Random(1, 11);
  Random playerSet = new Random(0, playerCount-1);
  ArrayList board = new ArrayList(playerCount);
  for(int x: board)
    {
      x=tokenNum;
      currentPlayer=playerSet;
    {
    return board;
}

public void distributeCurrentPlayerTokens(){
  /* to be answered in part (b) */
  Random spot= new Random(0,playerCount-1)
  int playerTokens = board.get(spot);
  for(int i=spot+1; playerTokens=0; i++)
  {
    board.get(i)
  }
}
